<!DOCTYPE html>
<!--[if lt IE 7]> <html class="no-js ie6 oldie" lang="en"> <![endif]-->
<!--[if IE 7]>    <html class="no-js ie7 oldie" lang="en"> <![endif]-->
<!--[if IE 8]>    <html class="no-js ie8 oldie" lang="en"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js" lang="en"> <!--<![endif]-->
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />

    <title>ForwardPosition</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/stylesheets/main1.css" />
  </head>
  <body>
    <div class="nav">
      <% if logged_in? && !session[:assistant] && User.find_by(:assistant_email => current_user.email)%><a href="/tasks/assistant">Enter Assistant Mode</a><% end %>
      <% if logged_in? && session[:assistant] == "yes" %><a href="/tasks/exit-assistant">Exit Assistant Mode</a><% end %>
      <% if logged_in? %><a href="/tasks">Task List</a><% end %>
      <% if logged_in? %><a href="/tasks/new">New Task</a><% end %>
      <% if logged_in? %><a href="/references">References</a><% end %>
      <% if logged_in? %><a href="/completed">Completed Tasks</a><% end %>
      <% if logged_in? %><a href="/users/<%= current_user.id %>">Settings</a><% end %>
      <% if logged_in? %><a href="/logout">Log Out</a><% end %>
    </div>
    <div class="wrapper">

        <%= yield %>

      <footer class="branding">
        <small>&copy; 2021 <strong>Forward Position</strong></small>
      </footer>
    </div>

    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1/jquery.min.js"></script>
    <!--[if lt IE 7]>
      <script src="//ajax.googleapis.com/ajax/libs/chrome-frame/1.0.2/CFInstall.min.js"></script>
      <script>window.attachEvent("onload",function(){CFInstall.check({mode:"overlay"})})</script>
    <![endif]-->
  </body>
</html>
